---
platform: linux

inputs:
- name: code

run:
  path: sh
  args:
    - -exc
    - |
      mv code/* gopath/src/${MODULE}
      export GOPATH=$(pwd)/gopath:${GOPATH}
      cd gopath/src/${MODULE}
      go version; echo
      go test -v ./...

params:
  MODULE: "dummy"